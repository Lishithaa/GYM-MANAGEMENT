# Copy to .env and adjust. Restart `yarn start` after any change.

# Dev server port (Create React App)
PORT=3000

# FastAPI origin: scheme + host + port only (no /api)
REACT_APP_BACKEND_URL=http://127.0.0.1:8000

# Do not set WDS_SOCKET_PORT for normal local dev (breaks hot reload WebSocket).
